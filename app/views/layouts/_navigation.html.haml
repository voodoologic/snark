= link_to "Snark.in", root_path, :class => 'brand'
%ul.nav
  - if user_signed_in?
    %li
      = link_to('Logout', destroy_user_session_path, :method=>'delete')
  - else
    %li
      = link_to('Log in w/ twitter', user_omniauth_authorize_path( :twitter ))
  - if user_signed_in?
    %li
      = link_to('Edit Account', edit_user_registration_path)
